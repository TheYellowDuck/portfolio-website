// HUD — PLANNED (not yet wired into GameCanvas).
//
// Intended purpose: a transient room-name title that fades in as the player
// crosses a doorway into a branch (e.g. "Projects", "Experience", "Archive").
// The Minimap already shows the active branch label in the corner; this would
// be a larger, center-screen flourish on room entry.
//
// Intended shape:
//   export default function HUD({ room }: { room: string | null }) { ... }

export {};
